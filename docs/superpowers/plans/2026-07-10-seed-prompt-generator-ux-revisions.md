# Seed Prompt Generator UX Revisions Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ship a new `html/Insights Forge (Seed Prompt Generator) - Draft.html` implementing six leadership-feedback UX changes, without touching the tracked bundle or the untracked `- Original.html` reference copy.

**Architecture:** `html/Insights Forge (Seed Prompt Generator).html` is a compiled Claude Artifact export: a bundler shell + asset dictionary (unchanged, line 186 of the file) wrapping a JSON-escaped string (line 194) that itself contains the real, hand-authored application — `class Component extends DCLogic { ... }` using a declarative template (`{{ }}` bindings, `<sc-if>`, `<sc-for>`). A new `tools/seed-prompt-generator-bundle.py` script extracts that string to a readable file for editing (`unpack`) and re-inserts an edited version into a fresh copy of the bundle (`pack`). Every feature task edits the readable extracted file, then re-packs and re-verifies.

**Tech Stack:** Plain HTML/CSS/JS (no build step, no framework beyond the artifact's own embedded React + custom `DCLogic`/templating runtime). Python 3 (stdlib only — `argparse`, `json`, `re`) for the unpack/pack tool.

## Global Constraints

- Never modify `html/Insights Forge (Seed Prompt Generator).html` (tracked) or `html/Insights Forge (Seed Prompt Generator) - Original.html` (untracked reference copy). All output goes to `html/Insights Forge (Seed Prompt Generator) - Draft.html`.
- Preserve every existing CSS custom property, style-helper method (`card()`, `pill()`, `box()`, `capRow()`, `scaleBtn()`, `chipStyle()`), and section layout exactly as-is except where a task explicitly changes it.
- All new copy uses curly quotes (“ ”) and en-dashes (–) to match the file's existing typographic style, not straight quotes/hyphens.
- No new runtime dependencies (no CDN scripts, no new fonts, no build tooling).

---

## Task 1: Bundle unpack/pack tool + extract editable source

**Files:**
- Create: `tools/seed-prompt-generator-bundle.py`
- Create: `html/seed-prompt-generator-src.html` (generated by running the tool below — this becomes the file every later task edits)

**Interfaces:**
- Produces: `tools/seed-prompt-generator-bundle.py unpack <bundle_path> <out_path>` — extracts the embedded Component source to a readable file.
- Produces: `tools/seed-prompt-generator-bundle.py pack <bundle_path> <src_path> <out_path>` — re-inserts an edited source file into a fresh copy of the bundle. All later tasks call this with `bundle_path` = the tracked original (`html/Insights Forge (Seed Prompt Generator).html`) and `out_path` = `html/Insights Forge (Seed Prompt Generator) - Draft.html`.

- [ ] **Step 1: Write the tool**

Create `tools/seed-prompt-generator-bundle.py`:

```python
#!/usr/bin/env python3
"""Unpack/pack the Component source embedded in a Claude Artifact bundle export.

The Seed Prompt Generator's .html files are Claude Artifact bundle exports:
a small harness plus an asset dictionary, with the actual application source
sitting as a single JSON-escaped string inside a
<script type="__bundler/template"> block. This tool extracts that string to
a readable file for editing, and re-inserts an edited version to produce a
new bundle.
"""
import argparse
import json
import re
import sys

TEMPLATE_OPEN = '<script type="__bundler/template">'
TEMPLATE_CLOSE = '</script>'


def find_template_line(lines):
    for i, line in enumerate(lines):
        if line.strip() == TEMPLATE_OPEN:
            if lines[i + 2].strip() != TEMPLATE_CLOSE:
                raise ValueError(
                    f"Expected {TEMPLATE_CLOSE!r} on line {i + 3}, found {lines[i + 2]!r}. "
                    "Bundle structure has changed since this tool was written."
                )
            return i + 1
    raise ValueError(f"No {TEMPLATE_OPEN!r} line found in {sys.argv}.")


def unpack(bundle_path, out_path):
    with open(bundle_path, encoding='utf-8') as f:
        lines = f.readlines()
    idx = find_template_line(lines)
    decoded = json.loads(lines[idx])
    with open(out_path, 'w', encoding='utf-8') as f:
        f.write(decoded)
    print(f"Unpacked {bundle_path!r} line {idx + 1} -> {out_path!r} ({len(decoded)} chars)")


def pack(bundle_path, src_path, out_path):
    with open(bundle_path, encoding='utf-8') as f:
        lines = f.readlines()
    idx = find_template_line(lines)
    with open(src_path, encoding='utf-8') as f:
        src = f.read()
    encoded = json.dumps(src)
    # The browser's HTML tokenizer scans for a literal `</script` sequence
    # inside ANY <script> element's raw text — including this one, which is
    # "just data" to us but not to the tokenizer. A literal `</script>`
    # anywhere in the decoded source (there are many, e.g. from nested
    # <style>/<script> tags in the app's own markup) would prematurely close
    # this wrapping <script type="__bundler/template"> tag and corrupt the
    # file. Escaping the slash keeps it valid JSON while breaking the match.
    encoded = re.sub(r'</(script)', r'<\\/\1', encoded, flags=re.IGNORECASE)
    lines[idx] = encoded + '\n'
    with open(out_path, 'w', encoding='utf-8') as f:
        f.writelines(lines)
    print(f"Packed {src_path!r} into {bundle_path!r} line {idx + 1} -> {out_path!r}")


def main():
    p = argparse.ArgumentParser(description=__doc__)
    sub = p.add_subparsers(dest='cmd', required=True)

    up = sub.add_parser('unpack', help='Extract the Component source to a readable file')
    up.add_argument('bundle_path')
    up.add_argument('out_path')

    pk = sub.add_parser('pack', help='Re-insert an edited Component source into a new bundle')
    pk.add_argument('bundle_path', help='Original bundle to use as the shell/asset source')
    pk.add_argument('src_path', help='Edited, readable Component source file')
    pk.add_argument('out_path', help='Path to write the new bundle file')

    args = p.parse_args()
    if args.cmd == 'unpack':
        unpack(args.bundle_path, args.out_path)
    elif args.cmd == 'pack':
        pack(args.bundle_path, args.src_path, args.out_path)


if __name__ == '__main__':
    main()
```

- [ ] **Step 2: Extract the working source**

Run:
```bash
python3 tools/seed-prompt-generator-bundle.py unpack "html/Insights Forge (Seed Prompt Generator).html" "html/seed-prompt-generator-src.html"
```
Expected: prints `Unpacked ... (N chars)` where N is roughly 113000-114000, and `html/seed-prompt-generator-src.html` exists and starts with `<!DOCTYPE html>`.

- [ ] **Step 3: Verify pack/unpack are true inverses (the test for this task)**

Run:
```bash
mkdir -p /tmp/if-bundle-test
python3 tools/seed-prompt-generator-bundle.py pack "html/Insights Forge (Seed Prompt Generator).html" "html/seed-prompt-generator-src.html" /tmp/if-bundle-test/roundtrip.html
python3 tools/seed-prompt-generator-bundle.py unpack /tmp/if-bundle-test/roundtrip.html /tmp/if-bundle-test/roundtrip-src.html
diff "html/seed-prompt-generator-src.html" /tmp/if-bundle-test/roundtrip-src.html
python3 -c "
lines_a = open('html/Insights Forge (Seed Prompt Generator).html', encoding='utf-8').readlines()
lines_b = open('/tmp/if-bundle-test/roundtrip.html', encoding='utf-8').readlines()
assert len(lines_a) == len(lines_b), (len(lines_a), len(lines_b))
diffs = [i for i in range(len(lines_a)) if lines_a[i] != lines_b[i]]
assert diffs == [193], f'expected only line 194 (index 193) to differ, got indices {diffs}'
print('OK: only the template line differs; all shell/asset lines are byte-identical')
"
rm -rf /tmp/if-bundle-test
```
Expected: `diff` prints nothing (the two decoded sources are identical — proves `pack` and `unpack` are inverses), and the Python check prints `OK: only the template line differs; all shell/asset lines are byte-identical`. If either check fails, the tool has a bug — do not proceed until this passes.

- [ ] **Step 4: Commit**

```bash
git add tools/seed-prompt-generator-bundle.py "html/seed-prompt-generator-src.html"
git commit -m "$(cat <<'EOF'
Add bundle unpack/pack tool and extract Seed Prompt Generator source

The Seed Prompt Generator .html files are compiled Claude Artifact
exports with the real application buried in a JSON-escaped string.
This tool makes that source readable and editable, and a new draft
bundle re-packable, without touching the shipped file.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 2: Intro / landing screen

**Files:**
- Modify: `html/seed-prompt-generator-src.html`

**Interfaces:**
- Consumes: nothing from other tasks.
- Produces: `state.showIntro` (boolean, default `true`); `dismissIntro()` method; `showIntro` / `showGuided` / `onGetStarted` keys on the object `renderVals()` returns — later tasks don't depend on these, but must not remove them.

- [ ] **Step 1: Add `showIntro` to initial state**

In `html/seed-prompt-generator-src.html`, find:
```js
    this.state = {
      theme: props.theme === 'light' ? 'light' : 'dark',
      step: 0,
```
Replace with:
```js
    this.state = {
      theme: props.theme === 'light' ? 'light' : 'dark',
      showIntro: true,
      step: 0,
```

- [ ] **Step 2: Add the `dismissIntro()` method**

Find:
```js
  goStep(n) { this.setState({ step: Math.max(0, Math.min(this.REVIEW, n)) }); }
```
Replace with:
```js
  goStep(n) { this.setState({ step: Math.max(0, Math.min(this.REVIEW, n)) }); }
  dismissIntro() { this.setState({ showIntro: false }); }
```

- [ ] **Step 3: Expose `showIntro`/`showGuided`/`onGetStarted` to the template**

Find:
```js
    return Object.assign({
      rootRef: this.rootRef,
      showGradient: this.props.showGradient !== false,
```
Replace with:
```js
    return Object.assign({
      rootRef: this.rootRef,
      showGradient: this.props.showGradient !== false,
      showIntro: st.showIntro,
      showGuided: !st.showIntro,
      onGetStarted: () => this.dismissIntro(),
```

- [ ] **Step 4: Add the intro screen markup and wrap the guided layout**

Find:
```html
    </div>
  </header>

  <!-- Main layout (guided) -->
  <div style="max-width:1440px; margin:14px auto 0; padding:0 24px 56px; display:grid; grid-template-columns:206px minmax(0,1fr) 348px; gap:20px; align-items:start;">
```
Replace with:
```html
    </div>
  </header>

  <sc-if value="{{ showIntro }}" hint-placeholder-val="{{ true }}">
  <div style="max-width:760px; margin:48px auto 0; padding:0 24px 56px;">
    <div style="background:var(--card,#212135); border:1px solid var(--border,#3b3b52); border-radius:14px; padding:36px 40px;">
      <h2 style="margin:0 0 12px; font-size:22px; font-weight:700; color:var(--text,#ebecff); letter-spacing:-.01em;">Start the engagement with the context already loaded.</h2>
      <p style="margin:0 0 18px; font-size:14.5px; color:var(--text-sub,#b1b2d2); line-height:1.55;">Phase 0 normally starts cold — the agent pulls the customer's story out of you one question at a time. This form flips that around: fill in what you know before the session, and it assembles a structured Phase 0 intake brief you paste into Claude Code to open the engagement already framed.</p>
      <ul style="margin:0 0 26px; padding:0 0 0 20px; font-size:13.5px; color:var(--text-sub,#b1b2d2); line-height:1.7;">
        <li>10 short sections — Must-haves unlock Copy/Download; everything else sharpens the result.</li>
        <li>Nothing here replaces Phase 0 — the agent still asks 1–3 rounds of clarifying questions and waits for your approval.</li>
        <li>Blank fields become “not provided” — a real gap the agent probes, never an invented answer.</li>
      </ul>
      <button onclick="{{ onGetStarted }}" style="background:var(--primary,#999bed); color:var(--on-primary,#1f2037); border:none; border-radius:100px; padding:13px 28px; font-size:15px; font-weight:600; cursor:pointer;" style-hover="background:var(--primary-hover,#b8bbff);">Get Started →</button>
    </div>
  </div>
  </sc-if>

  <sc-if value="{{ showGuided }}" hint-placeholder-val="{{ true }}">
  <!-- Main layout (guided) -->
  <div style="max-width:1440px; margin:14px auto 0; padding:0 24px 56px; display:grid; grid-template-columns:206px minmax(0,1fr) 348px; gap:20px; align-items:start;">
```

- [ ] **Step 5: Close the new `showGuided` wrapper**

Find:
```html
    </aside>

  </div>

  <footer style="max-width:1440px; margin:0 auto; padding:16px 24px 40px; display:flex; gap:14px; flex-wrap:wrap; font-size:11.5px; color:var(--text-faint,#8a8bad); border-top:1px solid var(--border,#3b3b52);">
```
Replace with:
```html
    </aside>

  </div>
  </sc-if>

  <footer style="max-width:1440px; margin:0 auto; padding:16px 24px 40px; display:flex; gap:14px; flex-wrap:wrap; font-size:11.5px; color:var(--text-faint,#8a8bad); border-top:1px solid var(--border,#3b3b52);">
```

- [ ] **Step 6: Pack and verify**

```bash
python3 tools/seed-prompt-generator-bundle.py pack "html/Insights Forge (Seed Prompt Generator).html" "html/seed-prompt-generator-src.html" "html/Insights Forge (Seed Prompt Generator) - Draft.html"
python3 -c "
import json
lines = open('html/Insights Forge (Seed Prompt Generator) - Draft.html', encoding='utf-8').readlines()
decoded = json.loads(lines[193])
assert 'Start the engagement with the context already loaded.' in decoded
assert 'showIntro' in decoded and 'showGuided' in decoded and 'onGetStarted' in decoded
print('OK: intro screen present and bundle line 194 is valid JSON')
"
```
Expected: `OK: intro screen present and bundle line 194 is valid JSON`.

- [ ] **Step 7: Commit**

```bash
git add "html/seed-prompt-generator-src.html" "html/Insights Forge (Seed Prompt Generator) - Draft.html"
git commit -m "$(cat <<'EOF'
Add intro/landing screen to Seed Prompt Generator draft

Explains why the form matters before Section 1 and ends in a Get
Started button, per leadership feedback.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 3: Customer size → explicit ACV label + named tiers

**Files:**
- Modify: `html/seed-prompt-generator-src.html`

**Interfaces:** none consumed or produced beyond the field itself; independent of Task 2.

- [ ] **Step 1: Relabel the field and replace the tier options**

Find:
```html
            <label style="display:flex; align-items:center; gap:8px; margin-bottom:6px; font-size:13px; font-weight:500; color:var(--text-sub,#b1b2d2);">Customer size <span style="font-size:9.5px; font-weight:700; letter-spacing:.05em; text-transform:uppercase; padding:1px 6px; border-radius:100px; color:var(--text-faint,#8a8bad); border:1px dashed var(--border,#3b3b52);">Nice</span></label>
            <select data-fid="size" value="{{ answers.size }}" onchange="{{ onInput }}" style="width:100%; background:var(--field,#1b1b30); color:var(--text,#ebecff); border:1px solid var(--border,#3b3b52); border-radius:6px; padding:10px 12px; font-size:14px; outline:none;" style-focus="border-color:var(--primary-border,#adb0ff); box-shadow:0 0 0 3px rgba(25,102,255,.25);">
              <option value="">— select —</option><option>Below $250k</option><option>Above $250K &amp; Below $1M</option><option>Above $1M</option>
            </select>
```
Replace with:
```html
            <label style="display:flex; align-items:center; gap:8px; margin-bottom:6px; font-size:13px; font-weight:500; color:var(--text-sub,#b1b2d2);">Customer size (ACV) <span style="font-size:9.5px; font-weight:700; letter-spacing:.05em; text-transform:uppercase; padding:1px 6px; border-radius:100px; color:var(--text-faint,#8a8bad); border:1px dashed var(--border,#3b3b52);">Nice</span></label>
            <select data-fid="size" value="{{ answers.size }}" onchange="{{ onInput }}" style="width:100%; background:var(--field,#1b1b30); color:var(--text,#ebecff); border:1px solid var(--border,#3b3b52); border-radius:6px; padding:10px 12px; font-size:14px; outline:none;" style-focus="border-color:var(--primary-border,#adb0ff); box-shadow:0 0 0 3px rgba(25,102,255,.25);">
              <option value="">— select —</option><option>Acceleration (&lt; $250K ACV)</option><option>Mid-Enterprise ($250K–$1M ACV)</option><option>Large Enterprise (&gt; $1M ACV)</option>
            </select>
```

- [ ] **Step 2: Pack and verify**

```bash
python3 tools/seed-prompt-generator-bundle.py pack "html/Insights Forge (Seed Prompt Generator).html" "html/seed-prompt-generator-src.html" "html/Insights Forge (Seed Prompt Generator) - Draft.html"
python3 -c "
import json
lines = open('html/Insights Forge (Seed Prompt Generator) - Draft.html', encoding='utf-8').readlines()
decoded = json.loads(lines[193])
assert 'Customer size (ACV)' in decoded
assert 'Acceleration (&lt; \$250K ACV)' in decoded
assert 'Mid-Enterprise (\$250K–\$1M ACV)' in decoded
assert 'Large Enterprise (&gt; \$1M ACV)' in decoded
print('OK: ACV label and tiers present')
"
```
Expected: `OK: ACV label and tiers present`.

- [ ] **Step 3: Commit**

```bash
git add "html/seed-prompt-generator-src.html" "html/Insights Forge (Seed Prompt Generator) - Draft.html"
git commit -m "$(cat <<'EOF'
Label customer size as ACV and use named tiers

Acceleration / Mid-Enterprise / Large Enterprise, same $250K/$1M
breakpoints as before — per leadership feedback.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 4: Clarify audience vs. stakeholders

**Files:**
- Modify: `html/seed-prompt-generator-src.html`

**Interfaces:** none; independent of Tasks 2-3. Note: the "application" ambiguity from the spec is already covered by the existing Section 7 copy (“Add every app in scope” appears in both the required and optional variants of `s6Why`) — verified during planning, no change needed there.

- [ ] **Step 1: Add an audience caption pointing to Stakeholders**

Find:
```html
            <sc-for list="{{ audienceOptions }}" as="o" hint-placeholder-count="3">
              <button onclick="{{ o.onClick }}" style="{{ o.style }}" style-hover="border-color:var(--border-hover,#4d4e66);">{{ o.label }}</button>
            </sc-for>
          </div>
          <div style="display:grid; grid-template-columns:1fr 1fr; gap:16px; margin-top:14px; align-items:start;">
            <div>
              <label style="display:block; margin-bottom:6px; font-size:13px; font-weight:500; color:var(--text-sub,#b1b2d2);">Meeting / read-time window</label>
```
Replace with:
```html
            <sc-for list="{{ audienceOptions }}" as="o" hint-placeholder-count="3">
              <button onclick="{{ o.onClick }}" style="{{ o.style }}" style-hover="border-color:var(--border-hover,#4d4e66);">{{ o.label }}</button>
            </sc-for>
          </div>
          <p style="margin:7px 0 0; font-size:11.5px; color:var(--text-faint,#8a8bad);">The general reader type for the deliverable's tone. For specific named people, see Stakeholders (Section 8).</p>
          <div style="display:grid; grid-template-columns:1fr 1fr; gap:16px; margin-top:14px; align-items:start;">
            <div>
              <label style="display:block; margin-bottom:6px; font-size:13px; font-weight:500; color:var(--text-sub,#b1b2d2);">Meeting / read-time window</label>
```

- [ ] **Step 2: Append a distinguishing clause to the Stakeholders Why line**

Find:
```html
        <p style="margin:8px 0 0; font-size:12px; color:var(--text-faint,#8a8bad);"><span style="color:var(--text-sub,#b1b2d2);">Why —</span> Phase 3 reads stakeholder profiles against each archetype. Add everyone who'll consume or influence the deliverable.</p>
```
Replace with:
```html
        <p style="margin:8px 0 0; font-size:12px; color:var(--text-faint,#8a8bad);"><span style="color:var(--text-sub,#b1b2d2);">Why —</span> Phase 3 reads stakeholder profiles against each archetype. Add everyone who'll consume or influence the deliverable. Different from the Audience picked in Engagement framing — this is specific people (or a role fallback) who'll read or influence the result.</p>
```

- [ ] **Step 3: Pack and verify**

```bash
python3 tools/seed-prompt-generator-bundle.py pack "html/Insights Forge (Seed Prompt Generator).html" "html/seed-prompt-generator-src.html" "html/Insights Forge (Seed Prompt Generator) - Draft.html"
python3 -c "
import json
lines = open('html/Insights Forge (Seed Prompt Generator) - Draft.html', encoding='utf-8').readlines()
decoded = json.loads(lines[193])
assert 'see Stakeholders (Section 8)' in decoded
assert 'Different from the Audience picked in Engagement framing' in decoded
print('OK: audience/stakeholder clarifying copy present')
"
```
Expected: `OK: audience/stakeholder clarifying copy present`.

- [ ] **Step 4: Commit**

```bash
git add "html/seed-prompt-generator-src.html" "html/Insights Forge (Seed Prompt Generator) - Draft.html"
git commit -m "$(cat <<'EOF'
Clarify audience vs. stakeholders in Seed Prompt Generator draft

Cross-references the two fields inline so they read as distinct
concepts instead of duplicates, per leadership feedback.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 5: Role archetype fallback (defaulted value, loosened Must-have gate)

**Files:**
- Modify: `html/seed-prompt-generator-src.html`

**Interfaces:**
- Produces: the archetype default string `'Stakeholder — role to be confirmed'`, used identically in three places (initial state, `addStk()`, and the `<select>`'s first `<option>`) — later tasks don't reference this, but if it's ever changed again it must be changed in all three places or the "always non-blank" invariant breaks.

- [ ] **Step 1: Default the initial stakeholder row's archetype**

Find:
```js
        apps:[ {name:'', rum:'', sr:''} ],
        stakeholders:[ {name:'', archetype:'', cares:''} ],
```
Replace with:
```js
        apps:[ {name:'', rum:'', sr:''} ],
        stakeholders:[ {name:'', archetype:'Stakeholder — role to be confirmed', cares:''} ],
```

- [ ] **Step 2: Default new rows added via "+ Add stakeholder"**

Find:
```js
  addStk() { this.setState(s => ({ answers: Object.assign({}, s.answers, { stakeholders: s.answers.stakeholders.concat([{ name:'', archetype:'', cares:'' }]) }) })); }
```
Replace with:
```js
  addStk() { this.setState(s => ({ answers: Object.assign({}, s.answers, { stakeholders: s.answers.stakeholders.concat([{ name:'', archetype:'Stakeholder — role to be confirmed', cares:'' }]) }) })); }
```

- [ ] **Step 3: Loosen the Must-have gate to accept archetype alone**

Find:
```js
    const stkOk = a.stakeholders.some(s => F(s.name) && s.archetype);
```
Replace with:
```js
    const stkOk = a.stakeholders.some(s => F(s.archetype));
```

- [ ] **Step 4: Update the missing-items message to match**

Find:
```js
    if (!a.stakeholders.some(s => s.name.trim() && s.archetype)) m.push('At least one stakeholder — name & archetype');
```
Replace with:
```js
    if (!a.stakeholders.some(s => s.archetype && s.archetype.trim())) m.push('At least one stakeholder role archetype');
```

- [ ] **Step 5: Replace the blank default option with the real fallback option**

Find:
```html
                <select value="{{ sk.archetype }}" onchange="{{ sk.onArch }}" style="width:100%; background:var(--card,#212135); color:var(--text,#ebecff); border:1px solid var(--border,#3b3b52); border-radius:6px; padding:9px 12px; font-size:14px; outline:none;" style-focus="border-color:var(--primary-border,#adb0ff); box-shadow:0 0 0 3px rgba(25,102,255,.25);">
                  <option value="">— select —</option><option>Executive Sponsor</option><option>Product Owner</option><option>SRE / Reliability Engineer</option><option>IT Operations Manager</option><option>Application Developer</option><option>Platform / DevOps Engineer</option><option>Security / Compliance Officer</option><option>Data / Analytics Lead</option><option>Marketing / Business</option><option>unsure</option>
                </select>
```
Replace with:
```html
                <select value="{{ sk.archetype }}" onchange="{{ sk.onArch }}" style="width:100%; background:var(--card,#212135); color:var(--text,#ebecff); border:1px solid var(--border,#3b3b52); border-radius:6px; padding:9px 12px; font-size:14px; outline:none;" style-focus="border-color:var(--primary-border,#adb0ff); box-shadow:0 0 0 3px rgba(25,102,255,.25);">
                  <option>Stakeholder — role to be confirmed</option><option>Executive Sponsor</option><option>Product Owner</option><option>SRE / Reliability Engineer</option><option>IT Operations Manager</option><option>Application Developer</option><option>Platform / DevOps Engineer</option><option>Security / Compliance Officer</option><option>Data / Analytics Lead</option><option>Marketing / Business</option><option>unsure</option>
                </select>
```

- [ ] **Step 6: Downgrade the Name field's tag from Must to Should**

Find:
```html
                <label style="display:flex; align-items:center; gap:6px; margin-bottom:6px; font-size:12.5px; font-weight:500; color:var(--text-sub,#b1b2d2);">Name &amp; title <span style="font-size:9px; font-weight:700; letter-spacing:.04em; text-transform:uppercase; padding:1px 5px; border-radius:100px; background:var(--primary-bg,#292944); color:var(--primary-border,#adb0ff);">Must</span></label>
```
Replace with:
```html
                <label style="display:flex; align-items:center; gap:6px; margin-bottom:6px; font-size:12.5px; font-weight:500; color:var(--text-sub,#b1b2d2);">Name &amp; title <span style="font-size:9px; font-weight:700; letter-spacing:.04em; text-transform:uppercase; padding:1px 5px; border-radius:100px; color:var(--text-sub,#b1b2d2); border:1px solid var(--border,#3b3b52);">Should</span></label>
```

- [ ] **Step 7: Pack and verify**

```bash
python3 tools/seed-prompt-generator-bundle.py pack "html/Insights Forge (Seed Prompt Generator).html" "html/seed-prompt-generator-src.html" "html/Insights Forge (Seed Prompt Generator) - Draft.html"
python3 -c "
import json
lines = open('html/Insights Forge (Seed Prompt Generator) - Draft.html', encoding='utf-8').readlines()
decoded = json.loads(lines[193])
assert decoded.count('Stakeholder — role to be confirmed') == 3, decoded.count('Stakeholder — role to be confirmed')
assert 'a.stakeholders.some(s => F(s.archetype))' in decoded
assert \"m.push('At least one stakeholder role archetype')\" in decoded
print('OK: archetype fallback default and loosened validation present')
"
```
Expected: `OK: archetype fallback default and loosened validation present`.

- [ ] **Step 8: Commit**

```bash
git add "html/seed-prompt-generator-src.html" "html/Insights Forge (Seed Prompt Generator) - Draft.html"
git commit -m "$(cat <<'EOF'
Add role-archetype fallback default for stakeholder Must-have

A stakeholder row now defaults its archetype to a real, non-blank
value instead of blank, and archetype alone (no name required)
satisfies the Stakeholders Must-have — per leadership feedback.
Name & title's tag downgrades from Must to Should since it no
longer gates export.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 6: Salesforce note near Stakeholders

**Files:**
- Modify: `html/seed-prompt-generator-src.html`

**Interfaces:** none; independent of other tasks (can run before or after Task 5 — inserts a new block, doesn't touch anything Task 5 changes).

- [ ] **Step 1: Insert the note after the Stakeholders Why line**

Find:
```html
        <p style="margin:8px 0 0; font-size:12px; color:var(--text-faint,#8a8bad);"><span style="color:var(--text-sub,#b1b2d2);">Why —</span> Phase 3 reads stakeholder profiles against each archetype. Add everyone who'll consume or influence the deliverable. Different from the Audience picked in Engagement framing — this is specific people (or a role fallback) who'll read or influence the result.</p>
        <sc-for list="{{ stakeholders }}" as="sk" hint-placeholder-count="1">
```
Replace with:
```html
        <p style="margin:8px 0 0; font-size:12px; color:var(--text-faint,#8a8bad);"><span style="color:var(--text-sub,#b1b2d2);">Why —</span> Phase 3 reads stakeholder profiles against each archetype. Add everyone who'll consume or influence the deliverable. Different from the Audience picked in Engagement framing — this is specific people (or a role fallback) who'll read or influence the result.</p>
        <div style="margin-top:10px; border-left:2px solid var(--primary-border,#adb0ff); padding:8px 12px; background:var(--field,#1b1b30); border-radius:0 8px 8px 0; font-size:12.5px; color:var(--text-sub,#b1b2d2); line-height:1.5;">Check Salesforce for the account team and named contacts — a real, named stakeholder sharpens the output far more than a role-only fallback.</div>
        <sc-for list="{{ stakeholders }}" as="sk" hint-placeholder-count="1">
```

Note: this step's anchor text depends on Task 4 Step 2 having already run (the Why line includes the "Different from the Audience..." clause added there). If Task 6 is executed before Task 4, use the pre-Task-4 Why line text instead — same insertion point either way, immediately before `<sc-for list="{{ stakeholders }}"`.

- [ ] **Step 2: Pack and verify**

```bash
python3 tools/seed-prompt-generator-bundle.py pack "html/Insights Forge (Seed Prompt Generator).html" "html/seed-prompt-generator-src.html" "html/Insights Forge (Seed Prompt Generator) - Draft.html"
python3 -c "
import json
lines = open('html/Insights Forge (Seed Prompt Generator) - Draft.html', encoding='utf-8').readlines()
decoded = json.loads(lines[193])
assert 'Check Salesforce for the account team and named contacts' in decoded
print('OK: Salesforce note present')
"
```
Expected: `OK: Salesforce note present`.

- [ ] **Step 3: Commit**

```bash
git add "html/seed-prompt-generator-src.html" "html/Insights Forge (Seed Prompt Generator) - Draft.html"
git commit -m "$(cat <<'EOF'
Add Salesforce nudge note under Stakeholders

Points analysts at Salesforce for named contacts and flags that a
real stakeholder beats the role-only fallback, per leadership
feedback.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 7: Status-dot colors — required/done/neutral, theme-aware

**Files:**
- Modify: `html/seed-prompt-generator-src.html`

**Interfaces:** `dotState()` return values change from `'done' | 'partial' | 'empty'` to `'done' | 'required' | 'neutral'`. Nothing else in the codebase reads `dotState()`'s return value except `dotFor()` in the same method, both updated together in this task.

- [ ] **Step 1: Collapse partial/empty into a single "required" state**

Find:
```js
  dotState(i) { const d = this._secData(i); if (d.req.length) { if (d.req.every(Boolean)) return 'done'; return this.secTouched(i) ? 'partial' : 'empty'; } return this.secTouched(i) ? 'done' : 'empty'; }
```
Replace with:
```js
  dotState(i) { const d = this._secData(i); if (d.req.length) { return d.req.every(Boolean) ? 'done' : 'required'; } return this.secTouched(i) ? 'done' : 'neutral'; }
```

- [ ] **Step 2: Update the dot color mapping**

Find:
```js
    const dotFor = (s) => 'width:8px; height:8px; border-radius:50%; flex-shrink:0; ' + (s === 'done' ? 'background:var(--success,#6fc3ba);' : (s === 'partial' ? 'background:var(--warning,#eea83f);' : 'border:1px solid var(--border-hover,#4d4e66);'));
```
Replace with:
```js
    const dotFor = (s) => 'width:8px; height:8px; border-radius:50%; flex-shrink:0; ' + (s === 'done' ? 'background:var(--success,#6fc3ba);' : (s === 'required' ? 'background:var(--warning,#eea83f);' : 'border:1px solid var(--border-hover,#4d4e66);'));
```

`--warning` is already `#eea83f` (amber) in the dark theme and `#c62239` (crimson) in the light theme (`THEMES.dark`/`THEMES.light` in the constructor, unchanged by this task) — the yellow-in-dark/red-in-light behavior comes from the existing theme system, no new CSS variable needed.

- [ ] **Step 3: Simplify the Review step's dot to match**

Find:
```js
    navItems.push({ num: '✓', title: 'Review & generate', onClick: () => this.goStep(this.REVIEW), style: navBase + (step === this.REVIEW ? navActive : ''), numStyle: numBase + (step === this.REVIEW ? numOn : numOff), dotStyle: dotFor(canExport ? 'done' : (missingList.length < 13 ? 'partial' : 'empty')) });
```
Replace with:
```js
    navItems.push({ num: '✓', title: 'Review & generate', onClick: () => this.goStep(this.REVIEW), style: navBase + (step === this.REVIEW ? navActive : ''), numStyle: numBase + (step === this.REVIEW ? numOn : numOff), dotStyle: dotFor(canExport ? 'done' : 'required') });
```

- [ ] **Step 4: Pack and verify**

```bash
python3 tools/seed-prompt-generator-bundle.py pack "html/Insights Forge (Seed Prompt Generator).html" "html/seed-prompt-generator-src.html" "html/Insights Forge (Seed Prompt Generator) - Draft.html"
python3 -c "
import json
lines = open('html/Insights Forge (Seed Prompt Generator) - Draft.html', encoding='utf-8').readlines()
decoded = json.loads(lines[193])
assert \"return d.req.every(Boolean) ? 'done' : 'required';\" in decoded
assert \"s === 'required' ? 'background:var(--warning,#eea83f);'\" in decoded
assert 'dotFor(canExport ? \'done\' : \'required\')' in decoded
assert \"'partial'\" not in decoded and \"'empty'\" not in decoded
print('OK: status dot logic simplified to done/required/neutral')
"
```
Expected: `OK: status dot logic simplified to done/required/neutral`.

- [ ] **Step 5: Commit**

```bash
git add "html/seed-prompt-generator-src.html" "html/Insights Forge (Seed Prompt Generator) - Draft.html"
git commit -m "$(cat <<'EOF'
Simplify status dots to done/required/neutral

Required-but-incomplete sections now show one color that's amber in
dark theme and crimson in light theme via the existing --warning
token, instead of a separate partial/empty split — per leadership
feedback.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 8: Final verification pass

**Files:** none modified — this task only exercises the file produced by Tasks 1-7.

- [ ] **Step 1: Confirm the draft bundle is well-formed**

```bash
python3 -c "
import json
lines = open('html/Insights Forge (Seed Prompt Generator) - Draft.html', encoding='utf-8').readlines()
decoded = json.loads(lines[193])
print(f'{len(lines)} lines in bundle, template line decodes to {len(decoded)} chars')
assert decoded.startswith('<!DOCTYPE html>')
print('OK: bundle is well-formed')
"
```
Expected: `OK: bundle is well-formed`.

- [ ] **Step 2: Open it in a browser and walk through it manually**

```bash
open "html/Insights Forge (Seed Prompt Generator) - Draft.html"
```

There's no headless-browser tooling in this repo, so this step is a manual walkthrough — confirm each of these by hand in the opened tab:
- Landing screen shows the new headline/body/list and a working **Get Started** button that reveals the guided layout.
- Section 3: field reads "Customer size (ACV)" with the three named tiers as options.
- Section 4: a caption under the audience buttons references Stakeholders (Section 8).
- Section 7: unchanged (confirms the "already covered" call in Task 4 was correct — re-read the existing Why text if in doubt).
- Section 8: the Why line ends with a sentence distinguishing stakeholders from Audience; a fresh row shows "Stakeholder — role to be confirmed" pre-selected (not blank); the Salesforce note is visible below the Why line; the Name field is tagged Should, not Must.
- With every other Must-have filled and Section 8 untouched (default archetype only, no name), Copy and Download both become enabled — confirms the loosened gate.
- Toggle the theme button (top right): a required-but-incomplete section's nav-rail dot is amber in dark mode and crimson in light mode.
- A fully optional, untouched section (e.g. Section 6 Out of scope, before Section 9/10 are touched) shows a neutral/outlined dot, not colored.

- [ ] **Step 3: Report back**

Tell the user the file is ready for manual review at `html/Insights Forge (Seed Prompt Generator) - Draft.html`, and ask them to confirm the walkthrough looks right before it replaces the tracked file (replacing the tracked file is explicitly out of scope for this plan — that's a separate, later decision).
